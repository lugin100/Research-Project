Overview over ToDos for Research Projects

- Read recommended papers ✓
- Summarize papers in own words 

- Generate diagram of workflow/components ✓

- Learn workflow on ML cluster ✓

- Acquire & overview training data ✓
- Understand transformation of training data in detail ✓
- Implement & conduct training data transformation ✓

- Understand training objective in detail ✓
- Set up training scaffolding ✓

- Understand Transformer architecture/ autoregression in detail ✓

- Adapt NLL objective for smoother learning ✓
- Add support for multiple transformer layers ✓
- Log GMM params ✓
- Set up model checkpointing ✓
- Custom Layer initialization ✓
- Implement early stopping ✓

- Set up inference from checkpoint ✓
- Visualization of predicted /ground truth data ✓
- Implement MSE Loss on weather data, correlation on coefficients ✓
- Calculate Metrics on zeros, white noise, ground truth data predictions ✓


- Slurm script for non-interactive training process ✓
- Try to train model on full data ✓

- Setup inference pipeline (as standalone script and after training) ✓
- Do inference for large model ✓
- Plot RMSE for coeffs and maps ✓
- Fix colorbar ranges and colormap ✓

- Fill out research project registration form ✓
- Calculate ACC Score
- Evaluate perplexity of categorical PIs ✓
- Draw a few samples, push forward through inv sh and do rank histogram (https://confluence.ecmwf.int/display/FUG/Section+12.B+Statistical+Concepts+-+Probabilistic+Data#Section12.BStatisticalConceptsProbabilisticData-RankHistogram(TalagrandDiagram))

- Find out cause of north pole artifact:
	- Does it appear in random model with N=120? No
	- How did forecast net get rid of it?	
	- Does it appear in new model?


- Model improvements:
	- Initialize positional encoding correctly
	- Change initialization for linear layers
	- Implement context length batching
	- Implement key-value-caching

